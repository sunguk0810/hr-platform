# Common environment variables for all application services
x-common-env: &common-env
  SPRING_PROFILES_ACTIVE: dev
  DB_HOST: postgres
  DB_PORT: "5432"
  DB_USERNAME: hr_saas
  DB_PASSWORD: hr_saas_password
  REDIS_HOST: redis
  REDIS_PORT: "6379"
  REDIS_PASSWORD: redis_password
  AWS_REGION: ap-northeast-2
  AWS_ACCESS_KEY_ID: test
  AWS_SECRET_ACCESS_KEY: test
  AWS_SNS_ENDPOINT: http://localstack:4566
  AWS_SQS_ENDPOINT: http://localstack:4566
  JWT_SECRET: hr-saas-jwt-secret-key-minimum-256-bits-for-hmac-sha256
  # Jaeger / OpenTelemetry tracing
  MANAGEMENT_TRACING_SAMPLING_PROBABILITY: "1.0"
  MANAGEMENT_OTLP_TRACING_ENDPOINT: http://jaeger:4318/v1/traces
  # Feign client URLs (inter-service communication)
  AUTH_SERVICE_URL: http://auth-service:8081
  TENANT_SERVICE_URL: http://tenant-service:8082
  ORGANIZATION_SERVICE_URL: http://organization-service:8083
  EMPLOYEE_SERVICE_URL: http://employee-service:8084
  ATTENDANCE_SERVICE_URL: http://attendance-service:8085
  APPROVAL_SERVICE_URL: http://approval-service:8086
  MDM_SERVICE_URL: http://mdm-service:8087
  NOTIFICATION_SERVICE_URL: http://notification-service:8088
  FILE_SERVICE_URL: http://file-service:8089
  APPOINTMENT_SERVICE_URL: http://appointment-service:8091
  CERTIFICATE_SERVICE_URL: http://certificate-service:8092
  RECRUITMENT_SERVICE_URL: http://recruitment-service:8093

services:
  # ===========================================
  # Infrastructure Services (always start)
  # ===========================================
  postgres:
    image: postgres:15-alpine
    container_name: hr-saas-postgres
    environment:
      POSTGRES_USER: hr_saas
      POSTGRES_PASSWORD: hr_saas_password
      POSTGRES_DB: hr_saas
    ports:
      - "15432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
      - ../scripts/sample-data:/sample-data:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hr_saas"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - hr-saas-network
  redis:
    image: redis:7-alpine
    platform: linux/amd64
    container_name: hr-saas-redis
    command: redis-server --requirepass redis_password
    ports:
      - "16379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "redis_password", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - hr-saas-network
  localstack:
    image: localstack/localstack:3.4
    container_name: hr-saas-localstack
    ports:
      - "14566:4566"
    environment:
      SERVICES: sns,sqs,s3
      AWS_DEFAULT_REGION: ap-northeast-2
    volumes:
      - "./localstack/init-aws.sh:/etc/localstack/init/ready.d/init-aws.sh"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:4566/_localstack/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - hr-saas-network
  jaeger:
    image: jaegertracing/all-in-one:1.52
    container_name: hr-saas-jaeger
    environment:
      - COLLECTOR_OTLP_ENABLED=true
    ports:
      - "16686:16686"
      - "14317:4317"
      - "14318:4318"
    networks:
      - hr-saas-network
  prometheus:
    image: prom/prometheus:v2.48.0
    container_name: hr-saas-prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
    ports:
      - "19090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    networks:
      - hr-saas-network
  grafana:
    image: grafana/grafana:10.2.0
    container_name: hr-saas-grafana
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    ports:
      - "13000:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning
    depends_on:
      - prometheus
    networks:
      - hr-saas-network
  # ===========================================
  # API Gateway (Traefik v3)
  # ===========================================
  traefik:
    image: traefik:v3.3
    container_name: hr-saas-traefik
    ports:
      - "18080:8080"
      - "18090:8090"
    volumes:
      - ./traefik/traefik.yml:/etc/traefik/traefik.yml:ro
      - ./traefik/dynamic:/etc/traefik/dynamic:ro
    networks:
      - hr-saas-network
  # ===========================================
  # Application Services (profiles: app)
  # ===========================================
  auth-service:
    build:
      context: ..
      dockerfile: docker/Dockerfile.service
      args:
        SERVICE_NAME: auth-service
        SERVER_PORT: 8081
    container_name: hr-saas-auth
    profiles: ["app"]
    ports:
      - "8081:8081"
    environment:
      <<: *common-env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.auth.rule=PathPrefix(`/api/v1/auth`)"
      - "traefik.http.routers.auth.entrypoints=web"
      - "traefik.http.routers.auth.middlewares=cors-headers@file,rate-limit-auth@file"
      - "traefik.http.services.auth.loadbalancer.server.port=8081"
    networks:
      - hr-saas-network
  tenant-service:
    build:
      context: ..
      dockerfile: docker/Dockerfile.service
      args:
        SERVICE_NAME: tenant-service
        SERVER_PORT: 8082
    container_name: hr-saas-tenant
    profiles: ["app"]
    ports:
      - "8082:8082"
    environment:
      <<: *common-env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.tenant.rule=PathPrefix(`/api/v1/tenants`)"
      - "traefik.http.routers.tenant.entrypoints=web"
      - "traefik.http.routers.tenant.middlewares=cors-headers@file,rate-limit@file"
      - "traefik.http.services.tenant.loadbalancer.server.port=8082"
    networks:
      - hr-saas-network
  organization-service:
    build:
      context: ..
      dockerfile: docker/Dockerfile.service
      args:
        SERVICE_NAME: organization-service
        SERVER_PORT: 8083
    container_name: hr-saas-organization
    profiles: ["app"]
    ports:
      - "8083:8083"
    environment:
      <<: *common-env
    depends_on:
      postgres:
        condition: service_healthy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.organization.rule=PathPrefix(`/api/v1/departments`) || PathPrefix(`/api/v1/positions`) || PathPrefix(`/api/v1/grades`) || PathPrefix(`/api/v1/announcements`) || PathPrefix(`/api/v1/committees`) || PathPrefix(`/api/v1/headcounts`)"
      - "traefik.http.routers.organization.entrypoints=web"
      - "traefik.http.routers.organization.middlewares=cors-headers@file,rate-limit@file"
      - "traefik.http.services.organization.loadbalancer.server.port=8083"
    networks:
      - hr-saas-network
  employee-service:
    build:
      context: ..
      dockerfile: docker/Dockerfile.service
      args:
        SERVICE_NAME: employee-service
        SERVER_PORT: 8084
    container_name: hr-saas-employee
    profiles: ["app"]
    ports:
      - "8084:8084"
    environment:
      <<: *common-env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.employee.rule=PathPrefix(`/api/v1/employees`) || PathPrefix(`/api/v1/transfers`) || PathPrefix(`/api/v1/condolences`) || PathPrefix(`/api/v1/employee-cards`) || PathPrefix(`/api/v1/profile`) || PathPrefix(`/api/v1/my-info`) || PathPrefix(`/api/v1/settings/employee-number-rule`)"
      - "traefik.http.routers.employee.entrypoints=web"
      - "traefik.http.routers.employee.middlewares=cors-headers@file,rate-limit@file"
      - "traefik.http.services.employee.loadbalancer.server.port=8084"
    networks:
      - hr-saas-network
  attendance-service:
    build:
      context: ..
      dockerfile: docker/Dockerfile.service
      args:
        SERVICE_NAME: attendance-service
        SERVER_PORT: 8085
    container_name: hr-saas-attendance
    profiles: ["app"]
    ports:
      - "8085:8085"
    environment:
      <<: *common-env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      localstack:
        condition: service_healthy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.attendance.rule=PathPrefix(`/api/v1/attendances`) || PathPrefix(`/api/v1/leaves`) || PathPrefix(`/api/v1/overtimes`)"
      - "traefik.http.routers.attendance.entrypoints=web"
      - "traefik.http.routers.attendance.middlewares=cors-headers@file,rate-limit@file"
      - "traefik.http.services.attendance.loadbalancer.server.port=8085"
    networks:
      - hr-saas-network
  approval-service:
    build:
      context: ..
      dockerfile: docker/Dockerfile.service
      args:
        SERVICE_NAME: approval-service
        SERVER_PORT: 8086
    container_name: hr-saas-approval
    profiles: ["app"]
    ports:
      - "8086:8086"
    environment:
      <<: *common-env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      localstack:
        condition: service_healthy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.approval.rule=PathPrefix(`/api/v1/approvals`)"
      - "traefik.http.routers.approval.entrypoints=web"
      - "traefik.http.routers.approval.middlewares=cors-headers@file,rate-limit@file"
      - "traefik.http.services.approval.loadbalancer.server.port=8086"
    networks:
      - hr-saas-network
  mdm-service:
    build:
      context: ..
      dockerfile: docker/Dockerfile.service
      args:
        SERVICE_NAME: mdm-service
        SERVER_PORT: 8087
    container_name: hr-saas-mdm
    profiles: ["app"]
    ports:
      - "8087:8087"
    environment:
      <<: *common-env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.mdm.rule=PathPrefix(`/api/v1/mdm`) || PathPrefix(`/api/v1/menus`) || PathPrefix(`/api/v1/admin/menus`)"
      - "traefik.http.routers.mdm.entrypoints=web"
      - "traefik.http.routers.mdm.middlewares=cors-headers@file,rate-limit@file"
      - "traefik.http.services.mdm.loadbalancer.server.port=8087"
    networks:
      - hr-saas-network
  notification-service:
    build:
      context: ..
      dockerfile: docker/Dockerfile.service
      args:
        SERVICE_NAME: notification-service
        SERVER_PORT: 8088
    container_name: hr-saas-notification
    profiles: ["app"]
    ports:
      - "8088:8088"
    environment:
      <<: *common-env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      localstack:
        condition: service_healthy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.notification.rule=PathPrefix(`/api/v1/notifications`) || PathPrefix(`/api/v1/announcements`) || PathPrefix(`/api/v1/help`) || PathPrefix(`/api/v1/settings/notification-channels`)"
      - "traefik.http.routers.notification.entrypoints=web"
      - "traefik.http.routers.notification.middlewares=cors-headers@file,rate-limit@file"
      - "traefik.http.services.notification.loadbalancer.server.port=8088"
    networks:
      - hr-saas-network
  file-service:
    build:
      context: ..
      dockerfile: docker/Dockerfile.service
      args:
        SERVICE_NAME: file-service
        SERVER_PORT: 8089
    container_name: hr-saas-file
    profiles: ["app"]
    ports:
      - "8089:8089"
    environment:
      <<: *common-env
      AWS_S3_ENDPOINT: http://localstack:4566
      AWS_S3_BUCKET: hr-platform-files
      FILE_STORAGE_DEFAULT: S3
    volumes:
      - file_storage:/app/storage
    depends_on:
      postgres:
        condition: service_healthy
      localstack:
        condition: service_healthy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.file.rule=PathPrefix(`/api/v1/files`) || PathPrefix(`/api/v1/settings/file-upload-policy`)"
      - "traefik.http.routers.file.entrypoints=web"
      - "traefik.http.routers.file.middlewares=cors-headers@file,rate-limit@file"
      - "traefik.http.services.file.loadbalancer.server.port=8089"
    networks:
      - hr-saas-network
  appointment-service:
    build:
      context: ..
      dockerfile: docker/Dockerfile.service
      args:
        SERVICE_NAME: appointment-service
        SERVER_PORT: 8091
    container_name: hr-saas-appointment
    profiles: ["app"]
    ports:
      - "8091:8091"
    environment:
      <<: *common-env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      localstack:
        condition: service_healthy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.appointment.rule=PathPrefix(`/api/v1/appointments`)"
      - "traefik.http.routers.appointment.entrypoints=web"
      - "traefik.http.routers.appointment.middlewares=cors-headers@file,rate-limit@file"
      - "traefik.http.services.appointment.loadbalancer.server.port=8091"
    networks:
      - hr-saas-network
  certificate-service:
    build:
      context: ..
      dockerfile: docker/Dockerfile.service
      args:
        SERVICE_NAME: certificate-service
        SERVER_PORT: 8092
    container_name: hr-saas-certificate
    profiles: ["app"]
    ports:
      - "8092:8092"
    environment:
      <<: *common-env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.certificate.rule=PathPrefix(`/api/v1/certificates`)"
      - "traefik.http.routers.certificate.entrypoints=web"
      - "traefik.http.routers.certificate.middlewares=cors-headers@file,rate-limit@file"
      - "traefik.http.services.certificate.loadbalancer.server.port=8092"
    networks:
      - hr-saas-network
  recruitment-service:
    build:
      context: ..
      dockerfile: docker/Dockerfile.service
      args:
        SERVICE_NAME: recruitment-service
        SERVER_PORT: 8093
    container_name: hr-saas-recruitment
    profiles: ["app"]
    ports:
      - "8093:8093"
    environment:
      <<: *common-env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      localstack:
        condition: service_healthy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.recruitment.rule=PathPrefix(`/api/v1/jobs`) || PathPrefix(`/api/v1/applications`) || PathPrefix(`/api/v1/interviews`) || PathPrefix(`/api/v1/offers`)"
      - "traefik.http.routers.recruitment.entrypoints=web"
      - "traefik.http.routers.recruitment.middlewares=cors-headers@file,rate-limit@file"
      - "traefik.http.services.recruitment.loadbalancer.server.port=8093"
    networks:
      - hr-saas-network
volumes:
  postgres_data:
  redis_data:
  prometheus_data:
  grafana_data:
  file_storage:
networks:
  hr-saas-network:
    name: hr-saas-network
