# ============================================
# HR SaaS Platform - Environment Variables
# ============================================
# Copy this file to .env and update with your actual values
#
# SECURITY WARNING:
#   - NEVER commit .env files to Git
#   - Change ALL default passwords in production
#   - Use strong, randomly generated secrets
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your values
#   docker-compose up
# ============================================

# ============================================
# PostgreSQL Database
# ============================================
# Username for PostgreSQL database
POSTGRES_USER=hr_saas
DB_USERNAME=hr_saas

# Password for PostgreSQL database
# CHANGE IN PRODUCTION! Use strong password (16+ chars, alphanumeric + symbols)
# Generate: openssl rand -base64 24
POSTGRES_PASSWORD=CHANGEME_DB_PASSWORD
DB_PASSWORD=CHANGEME_DB_PASSWORD

# Database name
POSTGRES_DB=hr_saas
DB_NAME=hr_saas

# ============================================
# Redis Cache
# ============================================
# Password for Redis cache
# CHANGE IN PRODUCTION! Use strong password (16+ chars)
# Generate: openssl rand -base64 24
REDIS_PASSWORD=CHANGEME_REDIS_PASSWORD

# ============================================
# JWT Authentication
# ============================================
# Secret key for JWT token signing
# MUST BE 256+ bits for HMAC-SHA256
# CHANGE IN PRODUCTION! Generate with:
#   openssl rand -base64 64
JWT_SECRET=CHANGEME_JWT_SECRET_MUST_BE_AT_LEAST_256_BITS_FOR_HMAC_SHA256

# ============================================
# AWS / LocalStack (Development)
# ============================================
# For local development with LocalStack
# In production, use IAM roles instead of credentials
AWS_REGION=ap-northeast-2
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# S3 Bucket for file storage
AWS_S3_BUCKET=hr-platform-files

# ============================================
# Monitoring & Observability
# ============================================
# Grafana admin credentials
# CHANGE IN PRODUCTION!
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=CHANGEME_GRAFANA_PASSWORD

# ============================================
# Application Settings
# ============================================
# Privacy encryption key (base64 encoded)
# CHANGE IN PRODUCTION! Generate with:
#   openssl rand -base64 32
ENCRYPTION_KEY=CHANGEME_ENCRYPTION_KEY_BASE64

# Spring profile (dev, staging, prod)
SPRING_PROFILES_ACTIVE=dev

# Tracing sampling rate (0.0 to 1.0)
# Use 0.1 in production to reduce overhead
TRACING_SAMPLING_PROBABILITY=1.0

# ============================================
# Production-Only (Ignore for local dev)
# ============================================
# Uncomment and configure for production deployment

# # Database host (use RDS endpoint in production)
# DB_HOST=hr-saas-db.xxxxxxxx.ap-northeast-2.rds.amazonaws.com
# DB_PORT=5432

# # Redis host (use ElastiCache endpoint in production)
# REDIS_HOST=hr-saas-redis.xxxxxx.ng.0001.apn2.cache.amazonaws.com
# REDIS_PORT=6379

# # SNS/SQS endpoints (remove for production, use AWS endpoints)
# # AWS_SNS_ENDPOINT=https://sns.ap-northeast-2.amazonaws.com
# # AWS_SQS_ENDPOINT=https://sqs.ap-northeast-2.amazonaws.com

# # S3 endpoint (remove for production)
# # AWS_S3_ENDPOINT=https://s3.ap-northeast-2.amazonaws.com

# ============================================
# Notes for Production Deployment
# ============================================
# 1. Use AWS Secrets Manager or Parameter Store for secrets
# 2. Use IAM roles for AWS access (no access keys)
# 3. Enable SSL/TLS for all connections (PostgreSQL, Redis)
# 4. Rotate secrets regularly (90-day policy recommended)
# 5. Use separate .env files per environment (dev/staging/prod)
# 6. Never log or expose secrets in application code
# ============================================
